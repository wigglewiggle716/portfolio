# دمج الصورة المهنية في Hero

تم استخراج الصورة الحالية كمرجع، ثم إنشاء صورة مهنية واقعية جديدة مع الحفاظ على هوية وملامح يوسف هشام. الصورة الجديدة تستخدم خلفية استوديو هادئة، جاكيتًا كحليًا وقميصًا فاتحًا، وتكوينًا رأسيًا مناسبًا للبروفايل.

تم تحسين الصورة للويب وتحويلها إلى `youssef-professional-portrait.jpg` بأبعاد 1000×1333 وحجم يقارب 114KB، ثم دمجها في `index.html` و`index-en.html` و`index-ar.html` باستخدام وسوم `alt` مناسبة لكل لغة.

على سطح المكتب ظهرت الصورة بجانب الـ Hero مع مساحة بيضاء متوازنة، بينما حافظت النسخة العربية على اتجاه RTL وظهرت الصورة في الجانب الأيسر بصريًا دون التأثير على قراءة النص أو الأزرار.

تم تطبيق تخطيط متجاوب: الصورة بجانب المحتوى على الشاشات الواسعة، وتنتقل إلى صف مرن أسفل أزرار الـ Hero على الشاشات الصغيرة.

الفحص البنيوي بعد الدمج: `validation=PASS` على 19 صفحة، مع `git diff --check` ناجح.
