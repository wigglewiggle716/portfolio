# دمج الصورة المهنية في Hero

تم اعتماد `portrait_option_3.png` بعد مقارنة ثلاث نسخ مولدة باستخدام صور يوسف كمرجع للهوية وصور الـ reference كمرجع للإضاءة والتكوين فقط.

تم تحويل Option 3 إلى `youssef-professional-portrait.jpg` بأبعاد 1000×1333 وحجم يقارب 129KB، ثم دمجها في `index.html` و`index-en.html` و`index-ar.html` باستخدام وسوم `alt` مناسبة لكل لغة.

تم تصغير خط عنوان الـ Hero إلى `clamp(34px,5.2vw,60px)`، وتقليل عرض النص التوضيحي، وإبقاء الصورة بجانب المحتوى على الشاشات الواسعة مع انتقالها إلى صف مرن على الموبايل.

المعاينة أظهرت الصورة كنصف جسم بالطول بجانب الـ Hero، مع الحفاظ على اتجاه RTL في العربية وتوازن جيد بين الصورة والنص والأزرار.

الفحص البنيوي بعد الدمج: `validation=PASS` على 19 صفحة، مع `git diff --check` ناجح.
