# Web Portrait Frame Adjustment

تم توسيع مساحة إطار الصورة في نسخة الويب لتصبح تقريبًا 420×380px على الشاشات الواسعة، مع وضع الصورة أعلى الإطار باستخدام `object-fit: contain` بحيث تظهر الصورة بالطول ويظل أسفل الإطار مساحة بيضاء كما في الإطار المحدد.

تمت إضافة breakpoints للشاشات المتوسطة حتى لا تتداخل الصورة مع عنوان الـ Hero. عند انخفاض العرض تتحول الصورة إلى عنصر داخل صف مرن بدلاً من التموضع المطلق، ثم تستمر إعدادات الموبايل الخاصة بالصورة.

المعاينة بعد الإصلاح أظهرت عدم تداخل الصورة مع عنوان الـ Hero على عرض المتصفح الحالي، وظهرت النسخة العربية باتجاه RTL مع الإطار والصورة في الجانب البصري المناسب.

الفحص البنيوي: `validation=PASS` على 19 صفحة، و`git diff --check` ناجح.
